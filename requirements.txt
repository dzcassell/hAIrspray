httpx[http2]==0.27.2
structlog==24.4.0
starlette==0.41.3
uvicorn==0.32.1

# Document extraction for the Prompt panel's attach-file feature.
# All three are pure-Python (no native deps), arm64+amd64 multi-arch
# safe, and pin to recent stable versions reviewed in April 2026.
pypdf==5.1.0
python-docx==1.1.2
openpyxl==3.1.5
python-multipart==0.0.18
